<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.proximipro.engage.android">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application>
        <activity
            android:name=".core.LocationSettingsActivity"
            android:launchMode="singleTask"
            android:theme="@style/LocusTheme" />

        <receiver
            android:name=".core.LocationBroadcastReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.proximipro.engage.android.receiver.LocationBroadcastReceiver.action.PROCESS_UPDATES" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".receiver.BootBroadcastReceiver"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".receiver.ServiceStopBroadcastReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.proximipro.engage.android.receiver.ServiceStopBroadcastReceiver" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".receiver.BackgroundScanResultReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.proximipro.engage.android.receiver.BackgroundScanResultReceiver" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
